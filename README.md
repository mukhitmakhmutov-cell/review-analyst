# Review Analyst — гибридный ML + LLM анализ отзывов

Курсовой проект по Data Science. Приложение классифицирует отзывы клиентов
(негатив / нейтрал / позитив) с помощью ML-модели и генерирует бизнес-отчёт
на естественном языке с помощью LLM.

## Что делает

1. **ML-классификатор** — линейная модель (LinearSVC) поверх TF-IDF (биграммы)
   обучена на 30 000 отзывов Yelp (3 класса). Для каждого отзыва выдаёт класс
   и вероятности по всем трём классам.
2. **LLM-отчёт** — локальная модель (Qwen, OpenAI-совместимый API) читает
   негативные отзывы и пишет структурированный отчёт на русском: основные
   жалобы + рекомендации, со ссылками на номера отзывов.
3. **Веб-интерфейс** — FastAPI + HTML/JS. Можно вставить отзывы вручную или
   перетащить файл (`.txt`, `.csv`, `.jsonl`, `.json`).

## Структура проекта

```
review_analyst/
├── app.py                  # FastAPI-приложение + встроенный UI
├── requirements.txt
├── .env                    # LLM_API_BASE / LLM_API_KEY / LLM_MODEL (не коммитить)
├── data/
│   ├── raw/yelp_reviews.csv    # датасет: 30k отзывов, 3 класса
│   └── sample_reviews.csv      # пример для проверки (12 отзывов, EN)
├── notebooks/
│   └── analysis.ipynb        # EDA + сравнение моделей + демо LLM-отчёта
├── model/
│   ├── best_pipeline.pkl     # обученный пайплайн (LinearSVC)
│   └── metrics.pkl           # таблица метрик
└── scripts/
    ├── download_data.py      # загрузка/подготовка датасета
    ├── build_notebook.py     # генерация ноутбука
    ├── compare_models.py     # сравнение моделей
    ├── gen_sample_file.py    # генерация примера через LLM
    └── test_api.py           # end-to-end тест API
```

## Метрики (test set)

| Модель | Acc | F1 (macro) | ROC AUC |
|---|---|---|---|
| **LinearSVC (биграммы, calibrated)** | 0.7453 | **0.7440** | **0.8938** |
| Logistic Regression (биграммы) | 0.7343 | 0.7345 | 0.8910 |
| XGBoost (биграммы, GPU) | 0.7050 | 0.7056 | 0.8720 |
| Naive Bayes (униграммы, baseline) | 0.6578 | 0.6608 | 0.8426 |

**Вывод:** на разреженных TF-IDF-признаках линейные модели обгоняют
градиентный бустинг — XGBoost теряет точность из-за переобучения на
высокоразмерных бинарных признаках. Это честный, воспроизводимый результат.

## Запуск

```bash
# 1. Зависимости
pip install -r requirements.txt

# 2. Настроить LLM (уже в .env, не коммитить)
#    LLM_API_BASE=https://...
#    LLM_API_KEY=...
#    LLM_MODEL=...

# 3. Запустить сервер
python -m uvicorn app:app --port 8000
```

Открыть **http://127.0.0.1:8000**, вставить отзывы или перетащить
`data/sample_reviews.csv`.

## Ограничения

- ML-модель обучена на **английских** отзывах — для корректной классификации
  вводите отзывы на английском (LLM-отчёт при этом пишется на русском).
- Лимиты: до 200 отзывов, до 5000 символов на отзыв, до 5 МБ на файл.
- LLM — thinking-модель: каждый запрос обязан передавать
  `extra_body={"chat_template_kwargs": {"enable_thinking": False}}` и
  `max_tokens >= 1600`, иначе `content` приходит пустым.
